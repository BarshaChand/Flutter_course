// Write a program in Dart that finds simple interest. Formula= (p * t * r) / 100

void main() {
double p = 5000; // IN Rs
double t = 5;  // IN Years
double r = 2; // rate of interest
double formula= (p * t * r) / 100;

print("The simple interest of $p Rs, $t years, and $r % is : $formula");
}